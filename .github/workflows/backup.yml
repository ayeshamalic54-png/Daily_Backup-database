- name: FINAL SECRET DEBUG
  env:
    DATABASE_URL: ${{ secrets.DATABASE_URL }}
  run: |
    echo "CHECK START"
    echo "LEN=${#DATABASE_URL}"
    if [ -z "$DATABASE_URL" ]; then
      echo "❌ STILL EMPTY"
      exit 1
    fi
    echo "✅ WORKING"
